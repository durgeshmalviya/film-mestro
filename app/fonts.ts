import { Allura, Poppins } from "next/font/google";

export const allura = Allura({
  subsets: ["latin"],
  weight: "400",
 
});

export const poppins = Poppins({
  subsets: ["latin"],
  weight: ["700", "800"],
});